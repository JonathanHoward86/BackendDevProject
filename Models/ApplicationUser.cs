using Microsoft.AspNetCore.Identity;

namespace MyEcommerceBackend.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Custom properties can be added here to extend the built-in IdentityUser class.
        // IdentityUser includes properties for things like username, password, email, etc.
    }
}
